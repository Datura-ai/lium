### Fixed
- `lium up` with filters (`--gpu`, `--count`, `--country`, `--ports`) now rents the Pareto-optimal node with the lowest total `$/h` (what the renter pays, so without `--count` a cheaper-per-GPU 8× node does not beat a 1× node) instead of the first one the API returned, prints the selected node and its total `$/h` before renting, and the help text says so. Explicit `NODE_ID` picks are unchanged. (DAH-2980)

### Changed
- `lium fund` help and prompts name your wallet and free alpha stake in plain payment terms; `lium ls`, `lium audit` and
  `lium describe` say "Lium's node checks". The SDK docstrings behind the docs.lium.io SDK reference (`AlphaQuote`,
  `add_wallet`, `convert_alpha`, `company_wallet`, `ls`, `pod_failure_cause`, `PodStartError`) use the same wording.

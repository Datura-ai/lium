### Fixed
- `lium provider …`: a portal 400 (or any other unmapped 4xx) is now `PORTAL_REQUEST_REJECTED` with the portal's own reason in the message (e.g. `Unsupported gpu type.`) instead of `PORTAL_SERVER_ERROR … Portal 5xx. Retry` (DAH-3111). Exit code for these is now 1 (user error) rather than 3 (portal fault).

### Fixed
- `lium provider …` human-mode listings that fall back to the generic table renderer no longer exit 1 with `ValueError: 'id' is not in list` (the column sort read `keys.index` while sorting the same list). (DAH-2936)

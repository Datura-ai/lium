### Fixed
- `lium fund` with an encrypted coldkey and no terminal (or `LIUM_NONINTERACTIVE=1`) let `bittensor_wallet` ask for the coldkey password itself, past `-y`; the prompt read an empty line and the command failed with "Wrong password" only after the balance call and the confirm. It now fails at once with `input_required` (exit 2), before any network call, naming the `BT_PW_…` variable `bittensor_wallet` reads for that keyfile. Set that variable (README, "Scripts and agents"), or fund from a coldkey without a password.

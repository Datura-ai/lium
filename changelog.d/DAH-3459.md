### Fixed
- `lium rm all` (the word `all` as TARGETS) now takes the same path as `lium rm --all`: it asks before removing every pod on a terminal, fails with `confirmation_required` (exit 2) when stdin is not a terminal or `LIUM_NONINTERACTIVE=1` is set, removes without a prompt only with `--yes`, and on an account with no pods says `No active pods` and exits 0 (it used to exit 5, `pod_not_found`). It used to remove every pod with no question in either case.

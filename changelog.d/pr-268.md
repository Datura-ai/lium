### Changed
- `lium ls` marks a Download / Upload figure that is a single speed-test sample from the node with a `~` (the tip and `lium ls --help` say so); a figure without the mark was checked or averaged by the platform. `--format json` rows carry `download_source` / `upload_source` (`measured`, `reported` or `null`). `--sort download` / `--sort upload` order by the figure the column shows; before they read the raw sample.

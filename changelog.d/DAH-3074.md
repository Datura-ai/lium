### Added
- `lium up <node> -c N` rents N GPUs of a splittable node (`Lium.up(gpu_count=)` sends `gpu_count`); before, `-c` with a node ID was rejected and the CLI had no way to rent part of a node although the API and web UI allow it.

### Added
- Docs: `docs/agents.md`, a single page an LLM agent or unattended script follows end to end (environment-variable auth, JSON output and the error envelope, exit codes, rent → exec → GPU stats → rsync → rm, pod gotchas). Every `lium …` line on the page (and the `--json` command list) is resolved against the click command tree by `test/test_agent_docs.py`, so a `lium …` line naming a command or flag this CLI does not have fails the suite. Linked from the README.

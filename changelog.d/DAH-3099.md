### Fixed
- `lium scp <pod> <file> <dir>` and `Lium.scp(remote=<dir>)`: a directory destination (an existing directory, or a path ending in `/`, which is created) now receives the file under its own name instead of failing with `Failed to upload`. Relative destinations (`out/`, `~/data/`) resolve against the login home, not `/`.

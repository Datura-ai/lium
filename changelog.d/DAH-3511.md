### Fixed
- `Lium.cp` / `lium cp` make the one-off transfer key on the client and place its private half on the source pod over the exec session's stdin. The destination now authorises the key the client made; the source pod's output no longer decides what the destination trusts, and the revoke removes that exact key rather than a comment marker (bug bounty report 6, ticket-0260).

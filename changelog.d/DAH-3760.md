### Fixed
- A 402 from the server is a budget refusal only when the error code is missing or `API_KEY_BUDGET_EXCEEDED`. A Stripe card-decline 402 is a plain API error, not a budget refusal.
- `lium rm a b --in 2h` prints the scheduled/failed summary first when one pod is a 402, then exits 6. The refused pod is not retried (no second charge).

### Security
- `lium config show` masks every `api_key` value and the `[session] token` the way `config get` and `config set` do (first 8 and last 4 characters), instead of printing `~/.lium/config.ini` in full. The first line still names the file that holds the full values.

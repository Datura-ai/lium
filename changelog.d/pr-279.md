### Changed
- CLI help and messages use American spelling: `lium provider machine` says "catalog", `lium cp` and `lium ps`
  help say "authorized", "serialize" and "honored", and the `lium cp` transfer-key error and warning say "authorize(d)".

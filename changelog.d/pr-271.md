### Added
- `lium ls` marks a node whose GPU power limit the provider set under the card's default with `↓W` after its Id and explains the mark in a legend line under the table; `--format json` and `ExecutorInfo` carry `gpu_power_limited`, `gpu_power_limit_w` and `gpu_power_limit_default_w` (`null` means the platform could not judge the node).
